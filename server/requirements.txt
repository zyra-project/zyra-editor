fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
zyra[api]>=0.1.45,<1
# Required by zyra CLI at runtime for date/period parsing (e.g. --since-period)
python-dateutil>=2.8,<3
# Required by zyra process pad-missing for image processing
Pillow>=10.0,<11
requests>=2.28,<3
# Test dependencies
httpx>=0.27,<1
